package model;

import java.util.UUID;

public class CheckInTests {

  private static final UUID FLIGHT_ID = UUID.randomUUID();
  //    private static final Seat SEAT = new Seat(SeatTests.CODE, SeatTests.ECONOMY_TYPE, SeatTests.FREE_STATUS, FLIGHT_ID);
}
